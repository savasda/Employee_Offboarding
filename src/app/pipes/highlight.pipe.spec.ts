/* tslint:disable:no-unused-variable */

import { HighlightPipe } from './highlight.pipe';

describe('Pipe: Highlighte', () => {
  it('create an instance', () => {
    const pipe = new HighlightPipe();
    expect(pipe).toBeTruthy();
  });
});
