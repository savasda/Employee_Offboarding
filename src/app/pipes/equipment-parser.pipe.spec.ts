/* tslint:disable:no-unused-variable */

import { EquipmentParserPipe } from './equipment-parser.pipe';

describe('Pipe: EquipmentParsere', () => {
  it('create an instance', () => {
    const pipe = new EquipmentParserPipe();
    expect(pipe).toBeTruthy();
  });
});
